import { PackageJSONConfig } from '@expo/config';
export declare function writeMetroConfig({ projectRoot, pkg, tempDir, }: {
    projectRoot: string;
    pkg: PackageJSONConfig;
    tempDir: string;
}): void;
